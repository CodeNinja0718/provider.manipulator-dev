import type { SxProps } from '@mui/material';

const styles: Record<string, SxProps> = {
  updateScheduleBtn: {
    mt: 32,
    maxWidth: 323,
    width: '100%',
  },
  emptyText: {
    textAlign: 'center',
    color: 'gray',
    mb: 30,
    fontSize: 24,
  },
};

export default styles;
